import '../styles/CV.scss';
import '../styles/Variables.scss';

const CV = () => {
  return (
    <div className="body_cv">
      <h1 className="cv_title">CV</h1>
    </div>
  );
};

export default CV;
